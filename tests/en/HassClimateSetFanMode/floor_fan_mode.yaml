---
# HassClimateSetFanMode - floor_fan_mode

language: "en"
floors:
  - name: "First Floor"

tests:
  - sentences:
      - "set the fan mode on the first floor to low"
      - "set the first floor fan mode to low"
    slots:
      floor: "First Floor"
      fan_mode: "low"
    response: "Fan mode set to low"
