---
# HassClimateSetFanMode - area_fan_mode

language: "en"
areas:
  - name: "Bedroom"

tests:
  - sentences:
      - "set the bedroom fan mode to low"
      - "set the fan mode in the bedroom to low"
    slots:
      area: "Bedroom"
      fan_mode: "low"
    response: "Fan mode set to low"
