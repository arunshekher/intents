---
# HassClimateSetFanMode - floor_fan_mode
# example: set the fan mode on the first floor to low
# slots:
# - {floor}
# - {fan_mode}

language: "en"
data:
  - sentences:
      - "<set> [the] fan mode (of|on) [<the>] {floor} [floor] to {fan_mode}"
      - "<set> [<the>] {floor} [floor] fan mode to {fan_mode}"
    example: "set the fan mode on the first floor to low"
    response: "default"
