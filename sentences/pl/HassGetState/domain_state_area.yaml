---
# HassGetState - domain_state_area
# example: czy wszystkie światła są włączone w salonie

language: "pl"
data:
  - sentences:
      - "<are_any> (<light>|<lights>) {on_off_states:state} <area>"
      - "<are_any> {on_off_states:state} (<light>|<lights>) <area>"
    example: "czy są jakieś światła włączone w salonie"
    inferred_domain: "light"
    response: "any"
  - sentences:
      - "<are_all> (<light>|<lights>) [jest|są] {on_off_states:state} <area>"
    example: "czy wszystkie światła są włączone w salonie"
    inferred_domain: "light"
    response: "all"
  - sentences:
      - "<which> (<light>|<lights>) (jest|są) {on_off_states:state} <area>"
    example: "które światła są włączone w salonie"
    inferred_domain: "light"
    response: "which"
  - sentences:
      - "<how_many> (<light>|<lights>) (jest|są) {on_off_states:state} <area>"
      - "<how_many> (jest|są) {on_off_states:state} (<light>|<lights>) <area>"
    example: "ile świateł jest włączonych w salonie"
    inferred_domain: "light"
    response: "how_many"
  - sentences:
      - "<are_any> (wentylator[y]|wiatrak[i]) {on_off_states:state} <area>"
      - "<are_any> {on_off_states:state} (wentylator[y]|wiatrak[i]) <area>"
    example: "czy są jakieś wentylatory włączone w salonie"
    inferred_domain: "fan"
    response: "any"
  - sentences:
      - "<are_all> (wentylator[ów|y]|wiatrak[ów|i]) [jest|są] {on_off_states:state} <area>"
    example: "czy wszystkie wentylatory są włączone w salonie"
    inferred_domain: "fan"
    response: "all"
  - sentences:
      - "<which> (wentylator[ów|y]|wiatrak[ów|i]) (jest|są) {on_off_states:state} <area>"
    example: "które wentylatory są włączone w salonie"
    inferred_domain: "fan"
    response: "which"
  - sentences:
      - "<how_many> (wentylator[ów|y]|wiatrak[ów|i]) (jest|są) {on_off_states:state} <area>"
      - "<how_many> (jest|są) {on_off_states:state} (wentylator[ów|y]|wiatrak[ów|i]) <area>"
    example: "ile wentylatorów jest włączonych w salonie"
    inferred_domain: "fan"
    response: "how_many"
  - sentences:
      - "<are_any> (przełącznik[ów|i]|kontakt[ów|y]) {on_off_states:state} <area>"
      - "<are_any> {on_off_states:state} (przełącznik[ów|i]|kontakt[ów|y]) <area>"
    example: "czy są jakieś przełączniki włączone w salonie"
    inferred_domain: "switch"
    response: "any"
  - sentences:
      - "<are_all> (przełącznik[ów|i]|kontakt[ów|y]) [jest|są] {on_off_states:state} <area>"
    example: "czy wszystkie przełączniki są włączone w salonie"
    inferred_domain: "switch"
    response: "all"
  - sentences:
      - "<which> (przełącznik[ów|i]|kontakt[ów|y]) (jest|są) {on_off_states:state} <area>"
    example: "które przełączniki są włączone w salonie"
    inferred_domain: "switch"
    response: "which"
  - sentences:
      - "<how_many> (przełącznik[ów|i]|kontakt[ów|y]) (jest|są) {on_off_states:state} <area>"
      - "<how_many> (jest|są) {on_off_states:state} (przełącznik[ów|i]|kontakt[ów|y]) <area>"
    example: "ile przełączników jest włączonych w salonie"
    inferred_domain: "switch"
    response: "how_many"
  - sentences:
      - "<are_any> <lockable> {lock_states:state} <area>"
      - "<are_any> {lock_states:state} <lockable> <area>"
    example: "czy są jakieś zamki zablokowane w salonie"
    inferred_domain: "lock"
    response: "any"
  - sentences:
      - "<are_all> <lockable> [jest|są] {lock_states:state} <area>"
    example: "czy wszystkie zamki są zablokowane w salonie"
    inferred_domain: "lock"
    response: "all"
  - sentences:
      - "<which> <lockable> (jest|są) {lock_states:state} <area>"
    example: "które zamki są zablokowane w salonie"
    inferred_domain: "lock"
    response: "which"
  - sentences:
      - "<how_many> <lockable> (jest|są) {lock_states:state} <area>"
      - "<how_many> (jest|są) {lock_states:state} <lockable> <area>"
    example: "ile zamków jest zablokowanych w salonie"
    inferred_domain: "lock"
    response: "how_many"
